# Production build env — loaded by Expo for release builds (inlined at build time).
# SP1: the deployed Cloudflare Worker leaderboard. Public URL, safe to commit.
EXPO_PUBLIC_LEADERBOARD_URL=https://tapclash-leaderboard.twigzzz28.workers.dev
