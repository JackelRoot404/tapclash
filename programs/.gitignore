node_modules
.anchor
test-ledger
.DS_Store

# Build artifacts are regenerated by `anchor build`.
target/

# IMPORTANT: the program keypair at target/deploy/tapclash_pools-keypair.json
# defines the program address (CZaaYuo8oNfW7XV8hxwugPw43DVHQQZ8zEoW2A2t2VwV).
# It is gitignored with the rest of target/. Back it up out-of-band — losing it
# only matters for a fresh first-deploy at the same address; upgrades use the
# program id + the wallet upgrade-authority, not this file.
keys/
